import { list } from "postcss";
import React from "react";

function itemsGroup(props: { items: Element[] }) {
  let items = props.items.map((item) => {});
  return <div className=" flex flex-row justify-center"></div>;
}

export default itemsGroup;
